{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug pdf_dowloader.py as module",
            "type": "debugpy",
            "request": "launch",
            "module": "rice_price_collector.downloader.pdf_dowloader",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}